node_modules/
dist/
.astro/
.playwright-mcp/

# Throwaway clone of the gh-pages branch, used by `npm run deploy`.
.deploy/

# Agent config for this machine. `*.local` below does NOT match
# `settings.local.json`, and that file has held paths into other projects.
.claude/

# Working screenshots.
shots/
*.png
!public/**/*.png

# Internal working documents. These are the sales playbook, the prospecting
# recipe, the plan and the running state — they are for the person selling this,
# not for the public repo the demo is served from.
SELLING.md
TODO.md
PLAN.md
STATE.md
HANDOFF.md
BRIEF-CORRECTION.md
CONTENT.md
RESKIN.md

*.local
.env
.env.*
!.env.example

# Debug debris from the 2026-08-12 mobile session. Nothing in the build
# references any of them; they are ignored rather than deleted so the next
# `git add -A` cannot push 178 KB of compiled CSS to a public repo.
built.css
talkpage.html
body.txt
